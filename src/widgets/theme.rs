use sim_draw::Canvas;
use std::cell::Cell;

////////////////////////////////////////////////////////////////////////////////////////////////////

pub struct Common {
   pub is_focused: bool,
   pub is_hover: bool,
   pub is_down: bool,
}

pub struct Button {
   pub base: Common,
   pub toggle: u8,
}

////////////////////////////////////////////////////////////////////////////////////////////////////

fn button(b: &Button, c: &mut Canvas) {}

pub static mut BUTTON: fn(&Button, &mut Canvas) = button;

////////////////////////////////////////////////////////////////////////////////////////////////////
